# UI元素迁移计划

## 一、当前游戏区外UI元素清单

### 1. 阶段标题提示（Overlay）
**位置**: `absolute inset-0` 覆盖整个游戏区  
**内容**: 
- 大标题文字（如"砖块生成阶段"、"子弹装填阶段"等）
- 黑色半透明背景
- 黄色边框
- 动画效果（zoom-in, pulse）

**问题**: 
- HTML元素覆盖在Canvas上，自适应时位置可能错位
- 依赖Tailwind CSS动画，与Canvas渲染不一致

---

### 2. 游戏区下方按钮组
**位置**: `mt-6` 游戏区下方  
**内容**:
- 🔄 重新开始
- 🧱 生成砖块
- ⬇️ 砖块下落
- ⏩ 下一阶段
- ⚙️ 子弹编程

**问题**:
- 固定在游戏区外，自适应时可能超出视口
- 按钮样式依赖Tailwind CSS，无法在Canvas内复用
- 点击事件需要单独处理

---

### 3. 状态信息面板
**位置**: `mt-6` 按钮组下方  
**内容**:
- 当前阶段
- 子弹数量
- 砖块数量
- 弹珠数量
- 待装填弹珠
- 模块库存（NORMAL, PIERCING, AOE等）

**问题**:
- 占用大量垂直空间
- 自适应时可能被裁剪
- 信息冗余（部分信息已在Canvas内显示）

---

### 4. 子弹编程编辑器（Modal）
**位置**: 独立Modal窗口  
**内容**:
- 子弹槽选择器
- 模块拖拽区域
- 模块库存显示
- 保存/关闭按钮

**问题**:
- 完全独立的HTML UI，无法集成到Canvas
- 拖拽交互复杂，需要重新实现

---

## 二、Canvas内已有UI元素

### 战斗场景
- ✅ 生命值、回合数、分数（左上角）
- ✅ 子弹槽显示（底部，能量、名称）
- ✅ 当前槽位高亮
- ✅ 瞄准线和轨迹预测
- ✅ 游戏结束提示

### 装填场景
- ✅ 场景标题（"⚙️ 子弹装填场景"）
- ✅ 子弹槽显示
- ✅ 缓冲器和弹珠
- ✅ 待发射弹珠数量

---

## 三、迁移策略

### 核心原则
1. **所有UI元素都在Canvas内渲染**，使用PixiJS Text和Graphics
2. **移除所有游戏区外的HTML元素**（按钮、面板、Overlay）
3. **保留子弹编程编辑器**作为独立Modal（复杂交互，暂不迁移）
4. **简化UI**，只保留必要信息，避免信息过载

---

### 方案A：最小化UI（推荐）

#### 游戏区内UI布局

```
┌─────────────────────────────────────────────┐
│ 生命:3/3  回合:5  分数:1200      [暂停] [⚙️] │  ← 顶部状态栏
├─────────────────────────────────────────────┤
│                                             │
│                                             │
│              游戏主区域                      │
│          （砖块、子弹、玩家等）               │
│                                             │
│                                             │
├─────────────────────────────────────────────┤
│ [槽位1: 能量80/90] [槽位2: 能量50/60] ...   │  ← 底部子弹槽
└─────────────────────────────────────────────┘
```

**顶部状态栏**（Canvas内）:
- 左侧：生命值、回合数、分数
- 右侧：暂停按钮、子弹编程按钮（Canvas内可点击区域）

**底部子弹槽**（Canvas内，已实现）:
- 显示3个子弹槽
- 当前槽位高亮
- 显示能量和名称

**阶段标题**（Canvas内）:
- 使用PixiJS Text渲染大标题
- 半透明黑色背景（Graphics）
- 黄色描边文字
- 简单的淡入淡出动画（alpha变化）

**移除的元素**:
- ❌ 游戏区下方的5个按钮（功能整合到Canvas内或快捷键）
- ❌ 状态信息面板（信息已在Canvas内显示）
- ❌ 阶段标题Overlay（改为Canvas内渲染）

**保留的元素**:
- ✅ 子弹编程编辑器Modal（独立窗口，复杂交互）

---

### 方案B：完整UI（备选）

如果需要保留所有功能按钮，可以在Canvas内实现按钮系统：

```
┌─────────────────────────────────────────────┐
│ 生命:3/3  回合:5  分数:1200                  │  ← 顶部状态栏
├─────────────────────────────────────────────┤
│                                             │
│              游戏主区域                      │
│                                             │
├─────────────────────────────────────────────┤
│ [槽位1] [槽位2] [槽位3]                      │  ← 子弹槽
├─────────────────────────────────────────────┤
│ [重新开始] [暂停] [下一阶段] [子弹编程]      │  ← 底部按钮栏
└─────────────────────────────────────────────┘
```

**优点**: 保留所有功能  
**缺点**: UI占用空间大，游戏区域变小

---

## 四、推荐实现方案（方案A）

### 4.1 顶部状态栏

**位置**: Canvas顶部，y=0-40  
**内容**:
- 左侧：`生命: 3/3  回合: 5  分数: 1200`
- 右侧：`[⏸️ 暂停] [⚙️ 编程]` （可点击区域）

**实现**:
```typescript
// 背景
const topBar = new PIXI.Graphics();
topBar.rect(0, 0, CANVAS_WIDTH, 40);
topBar.fill({ color: 0x000000, alpha: 0.5 });

// 状态文字
const statusText = new PIXI.Text({
  text: `生命: ${health}/${maxHealth}  回合: ${round}  分数: ${score}`,
  style: { fontSize: 16, fill: 0xffffff }
});
statusText.x = 10;
statusText.y = 10;

// 暂停按钮（可点击区域）
const pauseBtn = new PIXI.Graphics();
pauseBtn.rect(CANVAS_WIDTH - 150, 5, 60, 30);
pauseBtn.fill({ color: 0x333333 });
pauseBtn.eventMode = 'static';
pauseBtn.cursor = 'pointer';
pauseBtn.on('pointerdown', handlePause);

const pauseText = new PIXI.Text({
  text: '⏸️ 暂停',
  style: { fontSize: 14, fill: 0xffffff }
});
pauseText.x = CANVAS_WIDTH - 145;
pauseText.y = 12;
```

### 4.2 阶段标题（Canvas内）

**位置**: Canvas中心  
**显示时机**: 阶段切换时显示2秒后淡出  

**实现**:
```typescript
// 半透明黑色背景
const titleBg = new PIXI.Graphics();
titleBg.rect(0, 0, CANVAS_WIDTH, CANVAS_HEIGHT);
titleBg.fill({ color: 0x000000, alpha: 0.7 });

// 标题文字
const titleText = new PIXI.Text({
  text: '🧱 砖块生成阶段',
  style: {
    fontSize: 48,
    fill: 0xffff00,
    stroke: { color: 0xff8800, width: 4 },
    fontWeight: 'bold',
    align: 'center'
  }
});
titleText.anchor.set(0.5);
titleText.x = CANVAS_WIDTH / 2;
titleText.y = CANVAS_HEIGHT / 2;

// 淡入淡出动画
let alpha = 0;
const fadeIn = setInterval(() => {
  alpha += 0.1;
  titleBg.alpha = alpha * 0.7;
  titleText.alpha = alpha;
  if (alpha >= 1) {
    clearInterval(fadeIn);
    setTimeout(() => {
      // 2秒后淡出
      const fadeOut = setInterval(() => {
        alpha -= 0.1;
        titleBg.alpha = alpha * 0.7;
        titleText.alpha = alpha;
        if (alpha <= 0) {
          clearInterval(fadeOut);
          container.removeChild(titleBg, titleText);
        }
      }, 50);
    }, 2000);
  }
}, 50);
```

### 4.3 功能按钮整合

**移除的按钮及替代方案**:

| 原按钮 | 替代方案 |
|--------|---------|
| 🔄 重新开始 | 游戏结束时显示Canvas内按钮 |
| 🧱 生成砖块 | 自动触发，移除手动按钮 |
| ⬇️ 砖块下落 | 自动触发，移除手动按钮 |
| ⏩ 下一阶段 | 快捷键Space或自动触发 |
| ⚙️ 子弹编程 | 顶部状态栏右侧按钮 |

### 4.4 暂停菜单（Canvas内）

当点击暂停按钮时，在Canvas内显示暂停菜单：

```
┌─────────────────────────────┐
│                             │
│         游戏暂停            │
│                             │
│      [继续游戏]             │
│      [重新开始]             │
│      [音效设置]             │
│                             │
└─────────────────────────────┘
```

**实现**:
```typescript
// 暂停菜单背景
const pauseMenu = new PIXI.Graphics();
pauseMenu.rect(CANVAS_WIDTH/2 - 150, CANVAS_HEIGHT/2 - 150, 300, 300);
pauseMenu.fill({ color: 0x000000, alpha: 0.9 });
pauseMenu.stroke({ width: 4, color: 0xffff00 });

// 按钮
const resumeBtn = createButton('继续游戏', CANVAS_WIDTH/2, CANVAS_HEIGHT/2 - 50, handleResume);
const restartBtn = createButton('重新开始', CANVAS_WIDTH/2, CANVAS_HEIGHT/2, handleRestart);
const settingsBtn = createButton('音效设置', CANVAS_WIDTH/2, CANVAS_HEIGHT/2 + 50, handleSettings);
```

---

## 五、实现步骤

### 第一步：创建UIManager
创建 `client/src/game/UIManager.ts`，管理所有Canvas内UI元素：
- 顶部状态栏
- 阶段标题
- 暂停菜单
- 可点击按钮

### 第二步：修改Game.tsx
- 移除游戏区外的HTML按钮和面板
- 移除阶段标题Overlay
- 集成UIManager到渲染循环

### 第三步：实现Canvas内按钮系统
- 创建可点击的Graphics区域
- 添加hover效果
- 处理点击事件

### 第四步：实现暂停菜单
- 暂停游戏逻辑
- 显示暂停菜单UI
- 音效设置界面

### 第五步：测试和优化
- 测试自适应效果
- 测试所有交互功能
- 优化性能

---

## 六、最终效果

### 优点
✅ 所有UI在Canvas内，完美自适应  
✅ 统一的视觉风格（PixiJS渲染）  
✅ 简化UI，减少信息过载  
✅ 更好的游戏沉浸感  
✅ 移动端友好（可扩展触摸支持）  

### 保留
✅ 子弹编程编辑器（独立Modal，复杂交互）  

### 移除
❌ 游戏区外的5个按钮  
❌ 状态信息面板  
❌ HTML Overlay阶段标题  

---

## 七、工时估计

| 任务 | 估计时间 |
|------|---------|
| 创建UIManager | 1-2小时 |
| 实现顶部状态栏和按钮 | 1小时 |
| 实现Canvas内阶段标题 | 0.5小时 |
| 实现暂停菜单 | 1-2小时 |
| 移除HTML UI | 0.5小时 |
| 测试和优化 | 1小时 |
| **总计** | **5-7小时** |

---

**建议**: 采用**方案A（最小化UI）**，优先实现核心功能，后续可根据需要扩展。
